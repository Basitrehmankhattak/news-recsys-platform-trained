version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: newsrec
      POSTGRES_PASSWORD: newsrec
      POSTGRES_DB: newsrec
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
